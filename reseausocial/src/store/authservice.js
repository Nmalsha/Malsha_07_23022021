
/*
import api from  '../store/apiservice'

export default {
   signup (credentials){
return api().post('signup',credentials)
   } ,
   login (credentials){
      return api().post('signup',credentials)
         } 
}
*/